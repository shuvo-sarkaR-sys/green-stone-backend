# Gmail Configuration
GMAIL_USER=your-gmail@gmail.com
GMAIL_APP_PASSWORD=your-app-specific-password

# Recipient Emails (fallback to hardcoded if not provided)
RECIPIENT_EMAIL_1=md.sirforce@gmail.com
RECIPIENT_EMAIL_2=wpnajmul@gmail.com

# CORS Configuration (development allows all origins by default)
# For production, specify allowed origins (comma-separated or as JSON array)
CORS_ORIGIN=http://localhost:3000,http://yourdomain.com

# Server Configuration
PORT=3000
NODE_ENV=development

# Optional: Deployment
# VERCEL_URL=your-domain.com
